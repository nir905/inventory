import React from "react";
import styled from "styled-components";

const Wrapper = styled.h1`
  padding: 32px;
  margin: 0;
  color: #fff;
`;

const Header = () => {
  return <Wrapper>Inventory App</Wrapper>;
};

export default Header;
